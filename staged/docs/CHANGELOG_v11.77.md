# NIKE Lab Tools v11.77

## Population swimming — optional ROIs never trap you

The **"Draw optional spatial-filter ROIs"** step can now be **finished with no
ROIs** (the button reads "Finish / none"), the same fix already in Track one
worm. Because the ROIs there are genuinely optional, you can now proceed without
being forced to draw one; Cancel still leaves any existing ROIs untouched.

_Propagated from the Track one worm `allow_empty` fix during a cross-module
audit._
