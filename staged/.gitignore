
# Generated by the consolidation tool: one row per file for 1.5M+ files.
# The 20260807 plan was 339 MB and GitHub rejects anything over 100 MB, which
# is how it was noticed. These are run artifacts - the manifest that matters
# lives on the drive it describes and is mirrored to L, per the spec.
tools/drive_audit/consolidation_plan_*.csv
