# Endpoint egg counting

Scale-aware candidate detection for approximately 50 x 30 um eggs. Automatic candidates are never treated as final counts. The student reviews every image, toggles candidates, and may add missed eggs before reviewed CSV and summary outputs are written.
